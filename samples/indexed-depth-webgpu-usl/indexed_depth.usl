vertex VSIn {
  position: float4
  color:    float4
}

struct VSOut {
  position: float4 #position
  color:    float4
}

vert cube_vs(v: VSIn #stage_in) -> VSOut {
  v.position, v.color
}

frag cube_fs(i: VSOut #stage_in) -> float4 {
  return i.color
}
